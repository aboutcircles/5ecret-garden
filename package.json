{
  "name": "5ecret-garden",
  "private": true,
  "workspaces": [
    "packages/*",
    "circles-app"
  ],
  "scripts": {
    "build:packages": "npm --workspaces --if-present run build"
  },
  "engines": {
    "node": ">=18"
  }
}
