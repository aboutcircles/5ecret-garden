// Collapsed: Use SDK types and implementations instead of local copies.
// This file intentionally re-exports types only, to avoid duplication.

import type { Address, Hex } from '../safeSigner/types';
export type { Address, Hex };

export type { CustomDataLink } from '@circles-market/sdk';
export type {
  MinimalProductInput as MinimalProduct,
  MinimalOfferInput as MinimalOffer,
} from '@circles-market/sdk';
